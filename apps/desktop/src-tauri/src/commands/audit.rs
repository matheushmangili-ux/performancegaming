// Week 4: replace this stub with real powershell.exe spawn + JSONL stream parsing.
// Keeps the invoke_handler wiring in place so the frontend can ship early.

#[tauri::command]
pub fn run_audit_stub() -> &'static str {
    "pending: audit streaming implementation arrives in Week 3"
}
